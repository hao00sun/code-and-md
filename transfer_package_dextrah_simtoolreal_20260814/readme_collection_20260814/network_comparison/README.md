# Network Comparison Export 20260810

This folder contains the three LSTM network comparison experiment records for DEXTRAH.

Included:

- TensorBoard scalar CSV exports
- `summary.json` scalar statistics
- `params/` YAML parameter snapshots
- checkpoint index metadata only
- Chinese comparison summary

Excluded:

- model checkpoints and weights (`.pt`, `.pth`, `.safetensors`, `.onnx`, `.ckpt`, `.bin`)
